/** @format */

import React from "react";

function UserLikes() {
  return (
    <div>
      <h2>Likes</h2>
      {/* {likes ? (
        likes.map((like) => {
          return <div>{like.event_name} </div>;
        })
      ) : (
        <p> No likes yet</p>
      )} */}
    </div>
  );
}

export default UserLikes;
